doctype html
include ../all_included
html
    head
        meta(charset = 'utf-8')
        meta(name='viewport', content="initial-scale=1.0, width=device-width")
        title Friends
    body
        +header()
        main(date-title='Friends')
            +search()
            .container
                +user-profile('John Smith','ux designer','photo2','orange')
                +standart-button('send message','orange')
            .container
                +user-profile('Sarah Brown','developer','photo1','blue')
                +standart-button('send message','blue')
        footer
            center
                a(href="./page_registration.html") Sugn up
            