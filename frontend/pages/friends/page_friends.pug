include ../../all_included
doctype html
html
    head
        meta(charset = 'utf-8')
        meta(name='viewport', content="initial-scale=1.0, width=device-width")
        +favicons
        title Friends
    body
        +header()
        main(date-title='Friends')
            +search()
            .user-profile-container
                .user-profile-container__profile
                    +user-profile({ name:'John Smith', prof:'ux designer', src:'photo2', color:'orange' })
                .user-profile-container__button
                    +standart-button({ title:'send message', color:'orange' })
            .user-profile-container
                .user-profile-container__profile
                    +user-profile({ name:'Sarah Brown', prof:'developer', src:'photo1', color:'blue' })
                .user-profile-container__button
                    +standart-button({ title:'send message', color:'blue' })
        footer
            center
                a(href="./page_registration.html") Sugn up
            