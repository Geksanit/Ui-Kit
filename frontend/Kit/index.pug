include ../all_included
doctype html
html
    head
        meta(charset = 'utf-8')
        meta(name='viewport', content="initial-scale=1.0, width=device-width")
        title Ui-Kit
    body
        .block-line
            +standart-button({ title:'button', size:'small', color:'blue' })
            +standart-button
            +standart-button({ title:'button', size:'normal', color:'orange' })
            +standart-button({ title:'button', size:'small', color:'orange' })
        .block-line
            +arrow-button({ orientation:'left', disabled:true })
            +arrow-button({ orientation:'right', color:'orange', disabled:true })
            +arrow-button({ orientation:'left' })
            +arrow-button()
            +arrow-button({ orientation:'left', color:'orange' })
            +arrow-button({ orientation:'right', color:'orange' })
        .block-line
            +percentages(0)
            +percentages(38)
            +percentages(50)
            +percentages(62)
            +percentages(89)
            +percentages(100)
        .block-line
            +pie-chart(10,25,30)
            +pie-chart()
            +pie-chart(0,20,30)
        .block-line
            +slider
            +slider({ min:-5, max:12, value:1 })
            +slider-percent(75)
        .block-line
            +stages({ color:'orange' })
        .block-line
            +forma
        .block-line
            +search
        .block-line
            +search('notfound')
        .block-line
            +select('Schoose an option','option-1','option-2','option-3')
        .block-line
            +user-profile('John Smith','ux designer','photo2','orange')
        .block-line
            +user-profile('Sarah Brown','developer','photo1','blue')
        .block-line
            +news("It'is all going downhill",'24 august 2013','Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed condimentum aliquet venanatis. Etiam a ninsi nunc, a tincidunt eros.','orange')
        .block-line
            +events('24','Aug','this is title','Lorem ipsum dolor sit amet, consectetur adipiscing elit.')
        .block-line
            +map('-33.877685','151.207077','Meet Us !')
        .block-line
            +calendar
        .block-line
            +messaging('Sarah Brown','Hey, so are we cool to meet at the art gallery? Say 8pm','photo1')
        .block-line
            +video({ src:'Travel_New_Zeland', title:'New Zeland' })
        .block-line
        <script async defer src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBaRq2hOoLSW3DaHWf2aBP_xFlXdtYH0Oo&callback=initMap" >
        </script>
