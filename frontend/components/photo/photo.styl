photo($gray, $white)

  .photo
    $size = 5.875rem
    width $size
    height $size
    border-radius 100%
    box-sizing border-box
    border 0.31rem solid $gray[1]
    background $gray[1] no-repeat center
    text-align center
    line-height $size
    color $white
