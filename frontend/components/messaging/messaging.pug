mixin messaging(name,text)
    .messaging
        .messaging__top
            .messaging__name
                h2 #{name}
            .messaging__img
                .messaging__chat
                .messaging__photo
                .messaging__camera
        .messaging__bottom
            .messaging__buble #{text}
            textarea.messaging__message(name="message")
            +standart-button('reply')