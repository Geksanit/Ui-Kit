mixin radio(options)
    - //(name,value)
    if (!options || (typeof(options) !== 'object'))
        - options = {}
    if !options.name
        - options.name = 'radio'
    if !options.value
        - options.value = 'value'
    input(type="radio" class="radio" name=options.name value=options.value)