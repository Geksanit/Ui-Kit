mixin radio(options)
    - options = options || {}
    - options.name = options.name || 'radio'
    - options.value = options.value || 'value'
    - options.mix = options.mix || ''

    if options.checked
        input(checked type = "radio" class = "radio " + options.mix name = options.name value = options.value)
    else
        input(type = "radio" class = "radio " + options.mix name = options.name value = options.value)
