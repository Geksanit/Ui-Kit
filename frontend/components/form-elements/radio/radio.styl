radio($blue, $orange, $gray, $white)

  .radio
    position relative
    -webkit-appearance none
    -moz-appearance none
    background-color $gray[1]
    $size = 1.5rem
    width $size
    height $size
    border-radius 0.75rem
    border none
    outline none
    padding 0
    margin 0

    &:checked
      background-color $blue[0]

    &::before
      content ''
      position absolute
      top 0.5rem
      left 0.44rem
      width 0.5rem
      height 0.25rem
      border-bottom 0.125rem solid $gray[2]
      border-left 0.125rem solid $gray[2]
      transform rotate(-45deg)

    &:checked::before
      border-color $white
