mixin checkbox(options)
    - options = options || {}
    - options.name = options.name || 'radio'
    - options.value = options.value || 'value'

    input(checked = options.checked ? true : false type = "checkbox" class = "checkbox" name = options.name value = options.value)
