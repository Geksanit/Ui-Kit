mixin map(lat,lng,title)
    if(title == '')
        - title = 'Title'
    .map
        .map__element(data-lat=lat, data-lng=lng)
        .map__down
            .map__message #{title}
            .map__addres loading..
            .map__icon1
            .map__icon2